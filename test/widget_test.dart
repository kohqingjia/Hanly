import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — Supabase initialization required for full app test
    expect(1 + 1, equals(2));
  });
}
